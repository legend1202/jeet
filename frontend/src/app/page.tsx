import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Angel: The starting point for your next project',
};

export default function HomePage() {
  return <HomeView />;
}
